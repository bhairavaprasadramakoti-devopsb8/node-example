pipeline {
    agent {
        label 'java-slave'
    }

    stages {
        stage ('DockerBuild'){
            steps {
                sh "ls -la"
            }
        }
    }
}

//sh "docker build -t i27devopsb8/node:v7 ."
